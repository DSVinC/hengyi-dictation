# CHANGELOG

## v0.5.2 - 2026-04-14

### 修复 Gemini 验收问题

**修复项**：手动管理功能二次写入 + 输入框清空时序

| 问题 | 修复方案 |
|------|----------|
| `manualAddWordR1` 调用 `manualSetRound` 后二次写入 localStorage | 拆分出 `updateWordProgress` 静默函数，支持 `wrongCountIncrement` 参数一次性写入 |
| `alert` 阻塞导致交互不连贯 | 改为：写入 → 清空输入框 → alert → 重绘，顺序执行 |
| `manualAddWordMastered` 在重绘后清空输入框（DOM 已销毁） | 改为在重绘前清空 |

**变更文件**：`js/app.js`（28 行新增，14 行删除）

**验收**：Gemini CLI（acpx gemini exec）

---

## v0.5.1 - 2026-04-14

### 按钮防重复点击 + 英语音标 + 手动管理

**新增**：
- 听写完毕/错字词勾选完毕按钮一次性锁定
- 英语单词在选词页、复习区块、听写清单中展示音标 + 中文意思
- 进度总览手动管理（设R1/重置/手动添加/设为已掌握）

**变更文件**：`js/app.js` + `css/style.css`

---

## v0.5.0 - 2026-04-14

### 英语听写支持

- 英语听写清单展示音标 + 中文意思
- 数据填充：人教版PEP 2024三年级下册（81词）
